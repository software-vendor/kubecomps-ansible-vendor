# kubecomps-ansible-vendor

This repository generates deterministic archives for the three Kubespray
submodules pinned by `yunionio/kubecomps` v4.0.3. The archives are intended
for offline distribution packaging; they do not replace or modify the
upstream repositories.

Release assets are produced by GitHub Actions from the exact submodule commit
IDs recorded in the workflow.
